import {createAction, props} from '@ngrx/store';

export const UIToggleSidenav = createAction(
  '[UI] UIs Toggle Sidenav',
);

export const UICloseSidenav = createAction(
  '[UI] UIs Close Sidenav',
);



